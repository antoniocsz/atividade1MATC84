import React from 'react'
import { Image } from 'react-native'
import styles from '../styles/ManipulandoStyles'

const Logo = ({ source }) => <Image source={source} style={styles.logo} />

export default Logo
